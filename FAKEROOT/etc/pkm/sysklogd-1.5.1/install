### Does not take DESTDIR at install, this effectivly installs in fakeroot.
CMD=( install -vdm755 $FAKEROOT/$SDN/{sbin,man/man{5,8}} )
processCmd
[ $? -gt 0 ] && return 1
CMD=( make BINDIR=$FAKEROOT/$SDN/sbin MANDIR=$FAKEROOT/$SDN/man install )
processCmd
[ $? -gt 0 ] && return 1
return 0

  
