CMD=( autoreconf -fiv )
processCmd
[ $? -gt 0 ]   && return 1
CMD=( export FORCE_UNSAFE_CONFIGURE=1 )
processCmd
CMD=( ./configure --prefix=/usr --enable-no-install-program=kill,uptime )
processCmd
[ $? -gt 0 ]   && return 1
  return 0
  
