CMD=( make DESTDIR=$FAKEROOT/$SDN install )
processCmd
[ $? -gt 0 ] && return 1
CMD=( install -vdm755 $FAKEROOT/$SDN/lib )
processCmd
CMD=( mv -v $FAKEROOT/$SDN/usr/lib/libprocps.so.* $FAKEROOT/$SDN/lib )
processCmd

  
