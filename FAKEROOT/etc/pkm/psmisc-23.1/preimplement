[ ! -d $FAKEROOT/$SDN/bin ] && CMD=( install -vdm 644 $FAKEROOT/$SDN/bin ) && processCmd
CMD=( mv -v $FAKEROOT/$SDN/usr/bin/fuser   $FAKEROOT/$SDN/bin )
processCmd
CMD=( mv -v $FAKEROOT/$SDN/usr/bin/killall $FAKEROOT/$SDN/bin )
processCmd
return 0

