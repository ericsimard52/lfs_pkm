
CMD=( install -v -dm755 $FAKEROOT/$SDN/usr/lib/bfd-plugins )
processCmd
[ $? -gt 0 ] && return 1

CMD=( mkdir -pv $FAKEROOT/$SDN/usr/share/gdb/auto-load/usr/lib )
processCmd
[ $? -gt 0 ] && return 1

CMD=( mv -v $FAKEROOT/$SDN/usr/lib/*gdb.py $FAKEROOT/$SDN/usr/share/gdb/auto-load/usr/lib )
processCmd
[ $? -gt 0 ] && return 1
return 0
