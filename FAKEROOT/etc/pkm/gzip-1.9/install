CMD=( make DESTDIR=$FAKEROOT/$SDN install )
processCmd
[ $? -gt 0 ] && return 1
CMD=( install -vdm755 $FAKEROOT/$SDN/bin )
processCmd
CMD=( mv -v $FAKEROOT/$SDN/usr/bin/gzip $FAKEROOT/$SDN/bin )
processCmd
[ $? -gt 0 ] && return 1
return 0

