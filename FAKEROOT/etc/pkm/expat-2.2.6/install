CMD=( make DESTDIR=$FAKEROOT/$SDN install )
processCmd
[ $? -gt 0 ] && return 1

CMD=( install -v -m644 doc/*."{html,png,css}" $FAKEROOT/$SDN/usr/share/doc/expat-2.2.6 )
processCmd
[ $? -gt 0 ] && return 1
return 0
